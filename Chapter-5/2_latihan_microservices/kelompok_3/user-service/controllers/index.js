const user = require("./user");
const kelas_user = require("./kelas_user");
const kelas = require("./kelas");
const mentor = require("./mentor");

module.exports = { user, kelas_user, kelas, mentor };
