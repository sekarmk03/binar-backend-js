const auth = require('./auth');
const channel = require('./channel');
const video = require('./video');
const subscription = require('./subscription');

module.exports = { auth, channel, video, subscription };